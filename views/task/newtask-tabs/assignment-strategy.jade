.list-controller
  .form-group
    label(for="assignment_strategy_name") Strategy: 
    select.list-controller-name.form-control.input-small#assignment_strategy_name
      option(selected) None
      each strategy in assignmentStrategies
        option(
          data-name=strategy.name,
          data-type='strategy',
          data-params=JSON.stringify( strategy.params ),
          data-events=JSON.stringify( strategy.triggerOn )
          )= strategy.name
  .param-container
    h4 Triggers on
    .event-list
    h4 Parameters 
      span.badge 2
    .param-list